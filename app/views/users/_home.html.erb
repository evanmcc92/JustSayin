<% @user = current_user %>
<div class="right">
	<% if @feed_items.any? %>
	<div id="posts_stream">
		<%= render partial: 'browse/feed_item', collection: @feed_items %>
	</div>
	<% end %>
</div>
<div class="left">
	<%= render "users/left" %>
	<div id="trends">
		<h4>Trends</h4>
		<ul>
			<li>list of trends</li>
		</ul>
	</div>
</div>